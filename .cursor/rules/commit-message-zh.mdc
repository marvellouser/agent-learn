---
description: Git 提交信息使用中文撰写时的格式与约定
alwaysApply: true
---

# Git 提交信息（中文）

在本仓库中生成或撰写 **commit message** 时：

1. **语言**：正文使用简体中文；技术名词（包名、API、类名、环境变量等）可保留英文。
2. **结构**：
   - 第一行：`<类型>(<范围>): <简短摘要>`，摘要不超过约 50 字，句末一般不加句号。
   - 空一行后：用条目列出要点，以 `- ` 开头，说明「做了什么」与「为何需要」（若不明显）。
3. **类型**（与 Conventional Commits 对齐，标签可用英文）：`feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`chore`、`build` 等。
4. **范围**：可选，如子项目或模块目录名（例如 `hello-nes-langchain`）。

无需每次向用户确认是否使用中文；默认按上述规则用中文撰写提交说明。
